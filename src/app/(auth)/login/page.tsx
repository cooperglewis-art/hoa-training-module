import { LoginForm } from "@/components/auth/login-form";

export const metadata = {
  title: "Sign In | CCR Enforcement Training",
};

export default function LoginPage() {
  return <LoginForm />;
}
